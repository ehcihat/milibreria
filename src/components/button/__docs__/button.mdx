import { Canvas, Meta } from "@storybook/blocks";
import * as CustomButton from "./button.stories.tsx";

<Meta title="CustomButton" of={CustomButton} />

# CustomButton
Componente de botón con diferentes especializaciones.

## Example

<Canvas>
    <CustomButton.Primary />
</Canvas>

## Usage

```ts
import { CustomButton } from 'milibreria';

function Example() {
    return (
        <CustomButton
            text="Button primary"
            txtcolor="black"
            bgcolor="red"
            disabled={false}
            size="small"
            onClick={() => alert("Button Primary")}
        />
    );
}
